version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g react-scripts
  pre_build:
    commands:rd
      - npm installdd
  build:er
    commands:
      - CI=false npm run build
  post_build:ddffvergergerg
    commands:vfbvfv
      - mkdir -p build
      - cp appspec.yml build/
      - cp -r scripts dfvcxbuild/

artifacts:xc 
  base-directory: build
  files:
    - '**/*'cx
    - appspec.yml
    - scripts/**/*
  discard-paths: noxvxcvxv